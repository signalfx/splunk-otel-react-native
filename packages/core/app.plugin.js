/**
 * Expo Config Plugin entry point for @splunk/otel-react-native
 *
 * This plugin configures the native iOS and Android projects to work with
 * the Splunk OpenTelemetry React Native SDK when using `expo prebuild`.
 */
module.exports = require('./plugin/build');
